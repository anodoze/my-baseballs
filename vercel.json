{
  "buildCommand": "npm run build",
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/(.*)"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}